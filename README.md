# PrideMonthRPC

just a simple program to show a discord RPC for pride month. if you need more flags, dm me on discord @bunnygirlelysia
# Linux
either `./PrideMonthRPC-Linux` or you make a script to autostart it (as example systemd)
# Windows
should be fine if you just double click the .exe. i don't have windows to test it xD


then you select what flag you want to display and just keep the window open. the Timestamp should be set to your local time but idk what happens if it hits 24h xD
